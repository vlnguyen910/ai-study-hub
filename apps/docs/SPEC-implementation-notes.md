# SPEC HTML conversion notes

- Created a static HTML mirror of `apps/docs/SPEC.md` at `apps/docs/SPEC.html`.
- Preserved the current spec content, section order, tables, and status language without editorial changes.
- Added only presentation-layer HTML/CSS for readability; no product decisions were changed.
- Used semantic headings, tables, and lists so the artifact stays readable without depending on JavaScript.
- Added heading ids for navigation and future linking, but did not add any extra visible content beyond the source spec.
